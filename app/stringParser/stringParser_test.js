'use strict';

describe('myApp.stringParser module', function() {

  beforeEach(module('myApp.stringParser'));

  describe('stringParser controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var stringParserCtrl = $controller('stringParserCtrl');
      expect(stringParserCtrl).toBeDefined();
    }));

  });
});