'use strict';

describe('myApp.frontEnd module', function() {

  beforeEach(module('myApp.frontEnd'));

  describe('frontEnd controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var frontEndCtrl = $controller('frontEndCtrl');
      expect(frontEndCtrl).toBeDefined();
    }));

  });
});